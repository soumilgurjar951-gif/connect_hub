# ConnectHub - Requirements
# pip install -r requirements.txt se sab install hoga

# Core Django - 4.x ya 6.x dono chalega, >=4.2 rakho
Django>=4.2,<7.0

# Django REST Framework - API banane ke liye (mobile app / frontend ke liye)
djangorestframework==3.15.2

# Database
dj-database-url==2.1.0        # DATABASE_URL env var parse karne ke liye (Railway/Render)
psycopg2-binary==2.9.9        # PostgreSQL driver (production ke liye, local SQLite chalega)

# Image handling - ImageField ke liye compulsory hai
Pillow==10.4.0

# Env variables load karne ke liye
python-dotenv==1.0.1

# CORS - agar frontend alag domain pe ho
django-cors-headers==4.3.1

# Production static files
whitenoise==6.6.0
gunicorn==22.0.0              # Production server (Railway/Render use karta hai)

# Real-world prod (public launch)
django-storages==1.14.2
boto3==1.34.0

# Real-time (Optional - Week 5 me kaam ayega)
# MVP me polling se kaam chal jayega, WebSocket chahiye to ye uncomment karo:
channels==4.1.0
channels-redis==4.2.0
redis==5.0.5
daphne==4.0.0                 # ASGI server for WebSockets
